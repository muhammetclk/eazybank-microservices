

server:
  port: 9000
spring:
  application:
    name: cards
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  
  config:
    import:
    - application_qa.yml
    - application_prod.yml
  profiles:
    active:
    - qa


build:
  version: 3.0


cards:
  message: "Welcome to EazyBank cards related local APIs "
  contactDetails:
    name: "card Doe - Developer"
    email: "cardohn@eazybank.com"
  onCallSupport:
    - (555) 550-4321
    - (555) 523-7894